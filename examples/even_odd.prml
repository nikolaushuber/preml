rec even (x : int) -> bool = 
    if x == 0 then true else odd (x - 1) 

and odd (x : int) -> bool = 
    if x == 0 then false else even (x - 1) 

val test1 : bool = odd 15 

val test2 : bool = even 12 
